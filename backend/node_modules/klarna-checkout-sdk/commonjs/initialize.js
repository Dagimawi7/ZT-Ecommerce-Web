"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.initialize = void 0;
const klarnaUri_1 = require("./klarnaUri");
const setHeaders_1 = require("./setHeaders");
function initialize(init) {
    const { eid, secret, live } = init;
    if (!eid || !secret) {
        throw new Error('Invalid or missing credentials.');
    }
    const headers = (0, setHeaders_1.setHeaders)(init);
    const klarnaUri = live
        ? klarnaUri_1.klarnaUrls.live
        : klarnaUri_1.klarnaUrls.test;
    return {
        headers,
        klarnaUri,
    };
}
exports.initialize = initialize;
//# sourceMappingURL=initialize.js.map