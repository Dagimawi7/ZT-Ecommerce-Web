import axios from 'axios';
export async function createOrder(klarnaSDK, order) {
    const { klarnaUri, headers } = klarnaSDK;
    const { data } = await axios.post(klarnaUri, order, {
        headers: headers,
    });
    return data;
}
//# sourceMappingURL=createOrder.js.map