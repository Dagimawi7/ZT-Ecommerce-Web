import axios from 'axios';
export async function getOrder(klarnaSDK, orderId) {
    const { data } = await axios.get(`${klarnaSDK.klarnaUri}/${orderId}`, {
        headers: klarnaSDK.headers,
    });
    return data;
}
//# sourceMappingURL=getOrder.js.map