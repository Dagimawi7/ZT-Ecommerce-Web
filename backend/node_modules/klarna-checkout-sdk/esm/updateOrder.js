import axios from 'axios';
export async function updateOrder(klarnaSDK, klarnaOrder) {
    const { klarnaUri, headers } = klarnaSDK;
    const { order_id } = klarnaOrder;
    const { data } = (await axios.post(`${klarnaUri}/${order_id}`, klarnaOrder, {
        headers: headers,
    }));
    return data.order;
}
//# sourceMappingURL=updateOrder.js.map