import axios from 'axios';
export async function markAnOrderAsAborted(klarnaSDK, orderId) {
    const { klarnaUri, headers } = klarnaSDK;
    const { data } = await axios.post(`${klarnaUri}/${orderId}/abort`, null, {
        headers: headers,
    });
    return data.order;
}
//# sourceMappingURL=markAnOrderAsAborted.js.map